/**
 * Operadores Logicos 
 * 
 * && === AND === E
 * || === OR === OU
 * ! === NOT == NÃO
 */

let dinheiro = true;
let sol = true;

let resposta1 = (dinheiro && sol && true && true)

let resposta2 = (dinheiro || sol || false || false)


let usuario = "aaaa";
let senha = "123"

console.log(usuario === "aaaa" && senha === "123")
